import React, { Component } from 'react';

export default class TableInnerPagination extends Component {
  constructor(props) {
    super(props);
    this.state = {
    }
  }

  render() {
    return (
      <div>
        TableInnerPagination
            </div>
    );
  }
}
